const EventTypes = {
  SITUATION: 1,
  EPHEMERIS: 2
};

const SurveyTypes = {
  TEACHER: 'teacher',
  STUDENT: 'student'
};

const RoleTypes = {
  ADMIN: 'Administrator',
  USER: 'User'
};

module.exports = { EventTypes, SurveyTypes, RoleTypes };
